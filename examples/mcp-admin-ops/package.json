{
  "name": "agent-action-runner-mcp-admin-ops-example",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run build --workspace @agent-action-runner/mcp --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --workspace @agent-action-runner/mcp --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "test": "vitest run src",
    "pretypecheck": "npm run build --workspace @agent-action-runner/mcp --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-action-runner/core": "file:../../packages/core",
    "@agent-action-runner/mcp": "file:../../packages/mcp",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agent-action-runner-shared-admin-ops-example": "file:../shared-admin-ops"
  },
  "devDependencies": {
    "vitest": "^4.0.14"
  }
}
