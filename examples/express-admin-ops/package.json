{
  "name": "agent-action-runner-express-admin-ops-example",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run build --workspace @agent-action-runner/express --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --workspace @agent-action-runner/express --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "test": "vitest run src",
    "pretypecheck": "npm run build --workspace @agent-action-runner/express --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-action-runner/core": "file:../../packages/core",
    "@agent-action-runner/express": "file:../../packages/express",
    "@agent-action-runner/http": "file:../../packages/http",
    "agent-action-runner-shared-admin-ops-example": "file:../shared-admin-ops",
    "express": "^5.2.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "vitest": "^4.0.14"
  }
}
