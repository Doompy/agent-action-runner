{
  "name": "agent-action-runner-mcp-stdio-example",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run build --workspace @agent-action-runner/mcp --if-present",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --workspace @agent-action-runner/mcp --if-present",
    "test": "vitest run src",
    "pretypecheck": "npm run build --workspace @agent-action-runner/mcp --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-action-runner/core": "file:../../packages/core",
    "@agent-action-runner/mcp": "file:../../packages/mcp",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "vitest": "^4.0.14"
  }
}
