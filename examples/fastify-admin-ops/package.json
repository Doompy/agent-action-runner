{
  "name": "agent-action-runner-fastify-admin-ops-example",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run build --workspace @agent-action-runner/fastify --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --workspace @agent-action-runner/fastify --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "test": "vitest run src",
    "pretypecheck": "npm run build --workspace @agent-action-runner/fastify --if-present && npm run build --workspace agent-action-runner-shared-admin-ops-example --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-action-runner/core": "file:../../packages/core",
    "@agent-action-runner/fastify": "file:../../packages/fastify",
    "@agent-action-runner/http": "file:../../packages/http",
    "agent-action-runner-shared-admin-ops-example": "file:../shared-admin-ops",
    "fastify": "^5.8.5",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "vitest": "^4.0.14"
  }
}
