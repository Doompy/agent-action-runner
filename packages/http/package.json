{
  "name": "@agent-action-runner/http",
  "version": "0.2.0",
  "description": "Shared HTTP adapter utilities for Agent Action Runner.",
  "license": "Apache-2.0",
  "author": "Doompy",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Doompy/agent-action-runner.git",
    "directory": "packages/http"
  },
  "homepage": "https://github.com/Doompy/agent-action-runner#readme",
  "bugs": {
    "url": "https://github.com/Doompy/agent-action-runner/issues"
  },
  "keywords": [
    "agent",
    "actions",
    "workflow",
    "typescript",
    "http",
    "approval",
    "audit",
    "llm"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false,
  "scripts": {
    "prepack": "npm run build",
    "prebuild": "npm run build --workspace @agent-action-runner/core --if-present",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --workspace @agent-action-runner/core --if-present",
    "test": "vitest run",
    "pretypecheck": "npm run build --workspace @agent-action-runner/core --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@agent-action-runner/core": "^0.2.0"
  },
  "devDependencies": {
    "@agent-action-runner/core": "file:../core",
    "vitest": "^4.0.14",
    "zod": "^4.1.13"
  }
}
