{
  "name": "@agent-action-runner/cli",
  "version": "0.8.2",
  "description": "CLI for Agent Action Runner local development, manifest inspection, workflow validation, and MCP previews.",
  "license": "Apache-2.0",
  "author": "Doompy",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "agent-action-runner": "dist/index.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Doompy/agent-action-runner.git",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/Doompy/agent-action-runner#readme",
  "bugs": {
    "url": "https://github.com/Doompy/agent-action-runner/issues"
  },
  "keywords": [
    "typescript",
    "ai-agent",
    "llm",
    "workflow",
    "agent-workflow",
    "tool-calling",
    "mcp",
    "approval",
    "audit",
    "dry-run",
    "human-in-the-loop",
    "zod",
    "nestjs",
    "express",
    "fastify"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "sideEffects": false,
  "scripts": {
    "prepack": "npm run build",
    "prebuild": "npm run build --workspace @agent-action-runner/mcp --if-present",
    "build": "tsc -p tsconfig.build.json",
    "pretest": "npm run build --workspace @agent-action-runner/mcp --if-present",
    "test": "vitest run",
    "pretypecheck": "npm run build --workspace @agent-action-runner/mcp --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-action-runner/core": "^0.8.2",
    "@agent-action-runner/mcp": "^0.8.2",
    "zod": "^4.0.0"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "vitest": "^4.0.14"
  }
}
